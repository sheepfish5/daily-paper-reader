---
title: "AsmEvo: Agentic Assembly-Level Optimization of AMD GPU Kernels with Functional Equivalence Verification"
title_zh: AsmEvo：具备功能等价性验证的AMD GPU内核智能体汇编级优化
authors: "Ji Liu, Puyuan Yang, Rongzhang Zheng, Fan Wang, Jinglin Wang, Muhammad A. Awad, Mortis Huang, Andy Chang, Zekai Li, Zeping Li, Zihao An, Yue Liu, Yuchen Yang, Jianghui Wang, Chushi Chen, Ziqiong Liu, Fuwei Yang, Dong Li, Wen Heng Chung, Shengcai Liu, Emad Barsoum"
date: 2026-08-21
pdf: "https://arxiv.org/pdf/2608.20711v1"
tags: ["query:li"]
score: 9.0
evidence: AMD GPU内核的代理式汇编级优化与功能等价验证
tldr: 高性能机器学习系统常部署编译后的GPU内核，其源码不可用或与机器码差距大，现有源码级优化器难以直接作用。AsmEvo重建可重汇编表示，用长时程智能体提出汇编级编辑，经ABI保留重建与差分功能验证，并结合剖析指导热窗口编辑和保守回退。在MI308X上，AsmEvo改进29/30个KernelBench内核，几何平均加速1.35倍，最大达3.88倍；在MI300X生产负载上，所有AITer与vLLM/SGLang汇编内核均获提升，几何平均/最大分别为1.09x/1.31x与1.18x/1.34x，且功能等价。该工作证实汇编级无源码优化有效，为GPU内核性能挖潜提供新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.20711v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1781, \"height\": 689, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.20711v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 875, \"height\": 562, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.20711v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 823, \"height\": 424, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.20711v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1712, \"height\": 208, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.20711v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 850, \"height\": 529, \"label\": \"Table\"}]"
motivation: 已编译GPU二进制常无源码或与源码差距大，现有源码级优化器难以继续挖掘性能。
method: AsmEvo重建可重汇编表示，用长时程智能体提出汇编级编辑，经ABI保留重建、差分验证、剖析引导热窗口编辑与保守回退完成优化。
result: 在MI308X上，30个KernelBench内核中29个被改进，几何平均1.35倍、最大3.88倍；在MI300X生产负载上，AITer与vLLM/SGLang汇编内核全部加速，几何平均/最大达1.09x/1.31x和1.18x/1.34x，并保持功能等价。
conclusion: 汇编级无源码优化可行且有效，为GPU内核性能自动挖潜提供新路径。
---

## 摘要
高性能机器学习系统日益依赖GPU内核，而这些内核的可编辑源码可能不可用、由生成产生，或与最终机器码距离过远而难以暴露剩余优化空间。现有的LLM内核优化器和自动调优器主要针对CUDA、Triton、HIP或张量程序源码操作，并与参考实现进行验证。我们研究了一种更严格的设置：优化已编译的AMDGPU代码对象，其中部署的二进制文件是唯一的行为预言机。我们提出了AsmEvo，一种针对AMD GPU内核的智能体汇编级优化器。给定一个AMDGPU代码对象K0，AsmEvo重建一个可重新汇编的表示，通过长时程智能体提出低级编辑，重建保留ABI的优化对象，并仅在相同启动条件下与K0进行差异验证后才接受候选。AsmEvo结合了代码对象恢复、元数据感知重建、性能分析引导的热窗口编辑、正确性门控计时以及保守的就地补丁回退。我们在各种AMD GPU内核上对AsmEvo进行了大量实验。在MI308X上，AsmEvo改进了30个选定的KernelBench内核中的29个，达到1.35倍几何平均和3.88倍最大加速。在MI300X生产负载上，它改进了所有评估的AITer二进制文件和vLLM/SGLang Triton汇编内核，分别达到1.09倍/1.31倍和1.18倍/1.34倍的几何平均/最大加速，同时保持了功能等价性。

## Abstract
High-performance ML systems increasingly rely on GPU kernels whose editable source is unavailable, generated, or too distant from final machine code to expose remaining optimizations. Existing LLM kernel optimizers and autotuners mainly operate on CUDA, Triton, HIP, or tensor-program source and validate against reference implementations. We study a stricter setting: optimizing an already compiled AMDGPU code object, where the deployed binary is the only behavioral oracle.   We present AsmEvo, an agentic assembly-level optimizer for AMD GPU kernels. Given an AMDGPU code object K0, AsmEvo reconstructs a reassemblable representation, proposes low-level edits with a long-horizon agent, rebuilds an ABI-preserving optimized object, and accepts candidates only after differential verification against K0 under identical launches. AsmEvo combines code-object recovery, metadata-aware rebuilding, profiling-guided hot-window editing, correctness-gated timing, and conservative in-place patch fallback.   We conduct extensive experiments with AsmEvo on various AMD GPU kernels. On MI308X, AsmEvo improves 29 of 30 selected KernelBench kernels, reaching 1.35x geometric-mean and 3.88x maximum speedup. On MI300X production workloads, it improves all evaluated AITer binaries and vLLM/SGLang Triton assembly kernels, reaching 1.09x/1.31x and 1.18x/1.34x geometric-mean/maximum speedups, respectively, while preserving functional equivalence.

---

## 论文详细总结（自动生成）

## AsmEvo：具备功能等价性验证的AMD GPU内核智能体汇编级优化

### 1. 核心问题与整体含义（研究动机与背景）

- **背景**：现代机器学习系统大量依赖GPU内核，其最终性能不仅由高级调度决定，还受编译后低级决策（wait-counter放置、指令选择、寄存器分配、内核描述符和ABI可见元数据）影响。
- **痛点**：实际部署中，许多内核的**可编辑源码不可用、由编译生成、或与最终机器码相距过远**（如AITer二进制、Triton JIT缓存产物、vLLM/SGLang嵌入内核）。现有LLM内核优化器和自动调优器（KernelBench、CUDA/HIP/Triton优化等）均假设存在可编辑源码和独立参考实现，无法作用于“仅二进制”场景。
- **核心问题**：能否**仅以已编译的AMDGPU代码对象（HSACO）为输入、以原二进制为唯一行为预言机**，直接进行汇编级优化，且保证功能等价和ABI兼容？
- **意义**：AsmEvo首次将优化空间延伸到源码不可用的部署后阶段，为已编译二进制提供“安全挖潜”的新范式，与编译器、JIT、自动调优器和推理库形成互补而非替代。

### 2. 方法论：核心思想、关键技术细节与算法流程

#### 2.1 整体框架
AsmEvo将问题形式化为三个需求：
- **R1（ABI保持）**：保留符号、kernarg布局、启动语义和外部可见元数据，使原宿主启动器可不变调用。
- **R2（功能等价）**：对每个评估输入x，候选输出与原始预言机输出匹配（式1：余弦相似度cx ≥ θ、∞范数‖Δx‖∞ ≤ τ、整数缓冲区逐位一致ι）。
- **R3（延迟改进）**：降低实测延迟，提交需通过方差感知门控（式2：m⋆ = (1 + max(ϵ, k·cv))·ŝ(K⋆)）。

#### 2.2 关键组件与技术细节
- **代码对象恢复与往返保真**：从AMDGPU ELF容器重建可重汇编的AMDGCN汇编s(K₀)，恢复节、符号、notes、内核描述符、AMDGPU元数据，重符号化PC相对控制流；一次性往返门控（重新汇编+链接后与K₀逐字节比较，仅屏蔽链接决定字段）确保后续编辑基于忠实表示。
- **双层级差分预言机**（核心创新）：
  - **Tier 1 合成推断**：从描述符和元数据推断参数偏移、标量类型、指针角色、缓冲区和启动维度，生成确定性输入（含边界值、随机种子），分配带金丝雀的守卫区域以检测越界写。
  - **Tier 2 真实调度捕获**：对混合dtype、非连续张量、块表、嵌套指针等生产内核，拦截真实应用调度，记录kernarg缓冲区、网格/工作组维度、引用设备内存及调度前状态；候选在相同启动参数和内存内容下回放，绝对设备指针恢复至原虚拟地址以保持嵌套指针有效。
- **元数据感知重建**：重扫VGPR/SGPR/AGPR、LDS和scratch使用情况，重算内核描述符和元数据中的资源字段后重链接；kernarg布局、段大小、符号接口和启动语义保持冻结。
- **保守就地补丁回退**：仅当重建失败且编辑为资源中立、大小不增时使用，保留原始描述符和元数据；补丁候选仍须通过所有静态一致性、差分等价和性能门控。
- **热窗口定位**：通过硬件计数器、指令采样和静态分析定位停顿主导的指令窗口，减少搜索上下文并抑制无关修改，编辑后拼回完整汇编再全内核重建验证。
- **门控验证管道（Algorithm 1 GatedEval）**：严格顺序 —— 汇编有效性 → 静态ABI/资源一致性 → 功能等价 → 计时；正确性严格先于计时，防止“快但错”的候选获得正向信号。
- **长时程智能体搜索驱动（Algorithm 2）**：
  - **确定性控制器**拥有预算、工作区、重建/验证调用、提交决策、候选谱系和终止条件；LLM仅负责分析瓶颈、设计编辑、解读失败和重规划，**无法声明正确性或性能**。
  - **验证谱系树**：提交仅当g(c)通过（variance-aware threshold）；候选形成树状谱系而非单链，支持分支和回溯。
  - **多起点探索**：团队模式下由规划器分配正交优化方向（延迟隐藏、依赖减少、寄存器压力控制、访存重构、指令简化），从不同验证节点出发。
  - **验证组合**：共享基线的独立候选增量组合，每步重建+验证，组合须优于双亲；否则保留最优个体。
  - **记忆与反停滞**：存储成功见解、失败签名和候选哈希，检测停滞时重定向或升级为更广的多起点探索。

### 3. 实验设计：数据集、场景与基准

- **KernelBench Level 1**（15个）：激活、损失、reductions/norms、深度可分离卷积、matmul、池化。
- **KernelBench Level 2**（15个）：融合GEMM/matmul和卷积管道、归一化、激活、池化、reductions。
- **AITer代码对象**（4个）：FP8 blockscale MoE、FP8因果FMHA、BF16因果FMHA、BF16 FMHA反向。
- **Triton JIT HSACO**（4个）：vLLM A8W8 GEMM、vLLM fused MoE、SGLang fused MoE LoRA、SGLang AWQ GEMM。
- **基准设定**：未修改的原始二进制始终作为1.00×参考；所有报告加速均需通过功能等价验证后才计时。
- **对比方法**：未与其他汇编优化器（如STOKE、CuAsmRL）直接对比，而是与原始编译对象对比；与源码级优化器（KernelBench、GEAK等）定位为互补。KernelBench的K₀由GEAK风格agent（GPT-5.0）经5轮精炼HIP内核后编译得到，确保基线非弱基线（比PyTorch算子更快）。

### 4. 资源与算力

- **GPU硬件**：KernelBench L1/L2在 **MI308X**（CDNA3）上运行；AITer和Triton JIT在 **MI300X**（CDNA3）上运行。
- **搜索模型**：Claude Opus 4.8（provider默认解码）。
- **搜索配置**：默认 **4个GPU worker** 多起点并行搜索，每内核 **0.5天（12小时）墙钟预算**。
- **未明确说明**：未报告总GPU小时数、单内核实际平均耗时、能源消耗或总成本；也未报告训练类算力消耗（本文为推理搜索而非训练）。

### 5. 实验数量与充分性

- **总体规模**：30个KernelBench核 + 8个生产内核 = **38个内核**，覆盖大型语言模型最常见算子类型。
- **每个候选的验证**：等价性检查重复3次（θ=0.9999, τ=10⁻³），延迟取预热后R次事件计时中位数。
- **尝试/提交数量**：KernelBench L1平均119.3次尝试/内核、7.2次提交；L2平均156.4次尝试、6.8次提交；整体提交率仅**5.1%**，表明有用汇编重写稀疏。
- **端到端验证**：AITer替换通过其单元测试；vLLM/SGLang替换缓存HSACO并禁用重编译后通过框架测试，确认加速为drop-in加速而非仅回放增益。
- **充分性与客观性**：
  - **优点**：覆盖基准生成内核与生产二进制两大场景；每个报告加速均过功能验证；正确性先于计时防作弊；案例研究较详细。
  - **不足**：**未做消融实验**（未单独验证各组件如双层预言机、热窗口、组合验证、反停滞的贡献）；**未与其他汇编级优化器直接基准对比**；生产内核每类仅4个，样本量较小；AITer结果中FP8 FMHA仅1.007×、BF16 FMHA仅1.025×/1.049×，接近噪声边界（虽受方差感知门控保护）。

### 6. 主要结论与发现

- **KernelBench**：30个内核中29个被改进（97%），几何平均加速 **1.35×**，最大 **3.88×**（depthwise Conv2D）；L1全部改进达1.56× / 3.88×，L2改进14/15达1.17× / 2.55×（Matmul+Swish+Sum+GroupNorm）。L1中位数1.31×，L2中位数1.10×，反映融合内核剩余头部空间更小。
- **生产对象**：AITer全部4个改进，几何平均 **1.09×**、最大 **1.31×**（FP8 blockscale MoE）；Triton JIT全部4个改进，几何平均 **1.18×**、最大 **1.34×**（vLLM fused MoE）。
- **核心结论**：已部署的AMDGPU二进制中仍存在可恢复的低级优化空间（调度长延迟访存、wait-counter放置、缓存提示、buffer-load变体、地址计算精简等），且智能体搜索在确定性门控下可安全利用这些空间。
- **方法论发现**：仅5.1%的候选成为验证提交，证明汇编级优化解空间极为稀疏，正确性门控必不可少。

### 7. 优点

- **问题设定新颖**：首次系统研究“源码不可用、二进制唯一预言机”的AMDGPU内核汇编级优化，填补源码级优化器覆盖不到的空白。
- **正确性保障设计严谨**：正确性严格先于计时，“快但错”的候选无法获得正信号；确定性控制器与LLM智能体职责分离，LLM无法声明正确性或性能；提交采用方差感知阈值防测量噪声污染。
- **双层差分预言机实用性强**：合成推断覆盖简单内核，真实调度捕获处理指针丰富/混合dtype的生产内核，并保留原虚拟地址使嵌套指针有效。
- **往返保真机制可靠**：一次性字节级往返门控保证后续编辑基于忠实表示；就地补丁回退严格限制条件，仅改变重建机制而不放松接受标准。
- **搜索设计丰富**：验证谱系树（非破坏性单链）、多起点正交探索、验证组合、反停滞重定向，配合结构化失败反馈，提升搜索效率。
- **端到端验证充分**：不仅回放验证，还替换真实运行路径（AITer单元测试、vLLM/SGLang框架测试），证明优化的drop-in可用性。

### 8. 不足与局限

- **经验性等价而非形式化验证**：功能等价仅对评估的输入和启动配置成立，新shape需新的推断输入或调度捕获；存在未覆盖边界导致行为偏差的残余风险。
- **架构与应用范围受限**：当前恢复和重建实现校准于CDNA级AMD GPU；复杂应用状态依赖真实调度捕获，某些场景可能无法获得可靠预言机。
- **实验覆盖不足**：未做消融实验验证各组件贡献；未与STOKE、CuAsmRL等汇编级优化系统直接对比；生产内核样本量较小（每类4个），部分加速接近1.0×。
- **测量环境限制**：硬件共享且时钟无法锁定，虽用大启动配置和方差感知阈值缓解，但结果仍有噪声敏感性风险。
- **性能提升依赖头部空间**：可获得的加速高度

- **性能提升依赖头部空间**：可获得的加速高度高度依赖于原始编译器优化水平和内核本身的暴露并行性；对已充分优化或延迟隐藏接近饱和的内核（如部分AITer用例仅1.007×），收益可能收敛到1.0×附近，推广至其他架构或编译器产物时加速幅度未必可比。

### 9. 与现有工作的关系与定位

- **与源码级LLM内核优化器（KernelBench、GEAK等）**：AsmEvo不与其竞争，而是互补。源码级优化器在可编辑源码可用时负责高层算法/调度重构；AsmEvo作用于其编译产物或第三方二进制，解决源码缺失场景。
- **与传统编译器（LLVM AMDGPU后端、HIP编译器）**：AsmEvo在编译器已完成一轮优化之后继续挖掘机器码层面的剩余调度/指令选择空间；编译器仍负责代码生成主体，AsmEvo仅做保守、验证严格的后处理修整。
- **与自动调优框架（Autotuner、Triton调优）**：自动调优器在编译期替换代码变体；AsmEvo可在调优器不可用、只能替换最终对象文件的环境中工作，且不依赖源码参数搜索空间。
- **与经典汇编级重写系统（STOKE、CuAsmRL等）**：AsmEvo是首个面向AMDGPU/HSACO、将现代LLM智能体与确定性验证门控结合的迭代搜索系统；与STOKE对比，AsmEvo利用LLM先验产生高层意图的编辑候选，而非纯随机程序合成，搜索效率更高；但代价是需要LLM调用成本。

### 10. 总体评价与启示

- **总体评价**：AsmEvo在“仅二进制、无源码、无参考实现”的AMDGPU内核优化问题上给出了完整且工程上严谨的答案。其核心贡献不在于性能数字本身（1.09×–1.35×），而在于**证明了在确定性门控保护下，LLM驱动的汇编级智能体可以安全地打开编译器和源码层不可达的尾部优化空间**。
- **方法论启示**：
  - “正确性先于性能”的流水线设计是AI驱动代码优化的可靠范式：让LLM发挥创造性和模式识别优势，由确定性系统负责验证与选择，可有效规避幻觉和虚假改进。
  - 细粒度字节级往返门控与双层差分预言机的组合，使“没有源码”不再成为优化的绝对障碍，为闭源/遗留二进制优化提供了可复用的工程模板。
- **未来方向**：扩展到NVIDIA SASS/PTX及其他GPU架构；结合形式化等价性检查消除经验验证残余风险；针对更大规模生产内核建立持续集成式优化管道；探索更经济的搜索预算分配策略以降低LLM调用成本。
- **最终结论**：AsmEvo首次将大模型驱动的汇编级优化扩展到AMDGPU部署后二进制，以严格功能等价验证和ABI保持为前提，在所有测试场景中均获得正向加速，为GPU内核性能挖潜提供了一条安全、有效、可在生产环境中落地的全新路径。

（完）
