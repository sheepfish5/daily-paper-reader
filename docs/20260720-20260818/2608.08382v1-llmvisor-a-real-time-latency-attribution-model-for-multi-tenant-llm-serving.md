---
title: "LLMVisor: A Real-Time Latency Attribution Model for Multi-Tenant LLM Serving"
title_zh: LLMVisor：面向多租户LLM服务的实时延迟归因模型
authors: "Shuowei Jin, Xueshen Liu, Jiaxin Shan, Le Xu, Tieying Zhang, Liguang Xie, Z. Morley Mao"
date: 2026-08-09
pdf: "https://arxiv.org/pdf/2608.08382v1"
tags: ["query:li"]
score: 9.0
evidence: 面向多租户LLM服务的微秒级实时延迟归因模型，辅助调度决策
tldr: 多租户LLM推理中，协同批处理提升GPU利用率，但使每租户的延迟贡献无法透明核算，也限制了公平调度。LLMVisor利用roofline模型，将请求执行划分为内存/计算受限阶段，用与FLOPs及内存流量成比例的特征构造分段线性函数，实现批处理延迟的加性分解。在Llama 3.1-8B、Qwen 2.5-14B/32B及A100/H100上验证，相比token计数基线，prefill和decode的p90/p99误差分别最多降2.5x/3.3x和3.5x/4.4x，且以微秒级延迟运行在调度环内，为多租户粒度计费与调度提供基础。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.08382v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1141, \"height\": 447, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.08382v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1341, \"height\": 330, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.08382v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1343, \"height\": 330, \"label\": \"Table\"}]"
motivation: 多租户LLM服务中，批处理难以将延迟归因到每个租户，阻碍公平调度和精细化控制。
method: 基于roofline的归因模型，用FLOPs和内存I/O特征构造分段线性函数，将批延迟分解为每请求份额。
result: 在A100/H100上多种模型验证，相比token基线，p90/p99相对误差最大降低2.5x-4.4x，且微秒级开销。
conclusion: LLMVisor提供轻量准确的实时延迟归因，支撑多租户推理引擎的分片共享与调度优化。
---

## 摘要
随着LLM推理转向多租户GPU集群，协同批处理提高了吞吐量，但模糊了每个租户的使用情况并限制了控制能力。要实现推理引擎的分数共享，需要一种实时、逐请求的归因原语，该原语既要准确，又要足够轻量，以便在调度循环内运行。我们提出了LLMVisor，一种基于roofline引导的延迟归因模型，通过与FLOPs和内存I/O流量成比例的特征上的简洁分段线性形式，来捕获内存受限和计算受限阶段。LLMVisor将批处理延迟分解为可加性的、逐请求的份额，并以微秒级高效运行。我们在A100/H100 GPU上，针对Llama 3.1-8B和Qwen 2.5-14B/32B，在不同的张量并行度和工作负载混合情况下，对LLMVisor进行了评估。与基于token计数的基线相比，LLMVisor在prefill阶段取得了接近完美的R平方，并将p90和p99的相对误差分别降低了高达2.5倍和3.3倍；在decode阶段则分别降低了高达3.5倍和4.4倍，尽管存在批处理变异性和序列发散。

## Abstract
As LLM inference shifts to multi-tenant GPU clusters, co-batching improves throughput but obscures per-tenant usage and limits control. Enabling fractional sharing of the inference engine requires a real-time, per-request attribution primitive that is accurate and light enough to run inside the scheduling loop. We present LLMVisor, a roofline-guided latency attribution model that captures the memory-bound and compute-bound phases via a concise piecewise-linear form over features proportional to FLOPs and memory I/O traffic. LLMVisor decomposes batch latency into additive, per-request shares and runs efficiently at microsecond scale. We evaluate LLMVisor across Llama 3.1-8B and Qwen 2.5-14B/32B on A100/H100 GPUs under varying tensor parallelism and workload mixes. Compared to a token-count baseline, LLMVisor attains near-perfect R-squared and reduces relative error by up to 2.5x and 3.3x at p90 and p99, respectively, for prefill, and by up to 3.5x and 4.4x for decode, despite batching variability and sequence divergence.

---

## 论文详细总结（自动生成）

## LLMVisor：面向多租户LLM服务的实时延迟归因模型

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **背景**：LLM推理已大规模部署于多租户GPU集群，服务商常通过协同批处理（co-batching）提升吞吐量。但批量执行使每个租户请求的实际GPU资源消耗难以独立核算，租户因此无法获得公平的可量化调度机制。
- **核心问题**：如何在高频调度循环内（毫秒级决策粒度下），为每个请求提供**实时、精确、可加性分解（additive）**的GPU延迟归因，从而支撑多租户场景下的公平调度、SLO感知准入控制、计费与“what-if”容量规划？
- **核心主张**：实现推理引擎的"分数共享"（fractional sharing），需要一种**调度器内嵌的逐请求延迟归因原语**。作者认为理想的归因模型需要同时满足：准确性、归因一致性和执行高效性。现有方法（ML预测器轮询较慢、token计数代理粒度太粗）均无法兼顾这三者。

### 2. 论文提出的方法论：核心思想与关键技术细节

- **核心思想**：通过**roofline-guided特征构造**，将LMM推理的延迟建模为一个**分段线性函数**，特征选取与理论分析中计算的FLOPs和内存I/O成正比，将批延迟分解为各请求的**可加性份额**。
- **关键设计**：
  - **延迟模型**：
    \[
    T_B = \beta + a_1 \sum_{i \in B} p_i + a_2 \sum_{i \in B} c_i + a_3 \sum_{i \in B} p_i^2 + a_4 \sum_{i \in B} |B|
    \]
    其中 \(p_i\) 表示当前步骤请求 \(i\) 的处理token数，\(c_i\) 表示需加载的上下文长度（KV cache），\(|B|\) 为批次规模。\(\beta\) 捕获固定开销（如权重I/O），\(\sum p_i\) 模拟MLP与线性注意力计算量，\(\sum c_i\) 模拟KV cache流量，\(\sum p_i^2\) 建模二次自注意力，\(\sum |B|\) 反映批量利用率的提升。
  - **分段线性**：针对prefill（计算受限）与decode（内存受限）分别拟合两套系数（共4组参数）。
  - **系数拟合**：使用普通最小二乘法（OLS）对短时段冷启动采样（warm-up profiling）数据进行拟合，保留了解释性且代价较低。
  - **可加性归因**：因模型在线性项中引入的是 \( \sum_i \text{(特征)}\)，因此单请求的归因贡献可直接由式中对应特征项**在线性闭式**内分解计算。
  - **效率**：单请求微秒级运行时开销，比随机森林等常用ML预测器快约100倍，保证调度循环不被扰动。

### 3. 实验设计：数据集/场景、benchmark、对比方法

- **实验条件**：基于vLLM v0.7.3实现LLMVisor，利用其内置性能分析器采集每步真实耗时用于对比。覆盖prefill与decode两个阶段，测试场景包含张量并行（TP=1/2/4）、两个硬件平台和三种开源模型。
- **硬件与并行配置**：
  - H100 SXM 80GB（4卡服务器，TP=1/2/4，表中记为H1/H2/H4）；
  - A100 SXM 80GB（8卡服务器，TP=1/2，表中记为A1/A2）。
- **模型**：Llama 3.1-8B、Qwen 2.5-14B、Qwen 2.5-32B。
- **工作负载**：单次并发请求128~2048个；总token从4096逐步增至引擎KV cache容量的90%，并随机分配至请求，保证序列长度有较高方差，起到模拟多租户真实负载的效果。
- **评估指标**：R²（预测与真实延迟的线性相关性）、相对误差的p90与p99（评价高百分位精度）。
- **基线对比**：VTC（token计数模型）——即按token数线性归因，不建模批量大小与上下文负载。

### 4. 资源与算力

- **明确信息**：服务器配置为4×NVIDIA H100 SXM 80GB（104核vCPU/1800GiB内存）和8×NVIDIA A100 SXM 80GB（240核vCPU/1800GiB内存），覆盖TP=1/2/4的并行组合。
- **未明确披露**：论文未具体说明每次训练集、测试集划分比例，也未给出各实验的GPU运行时长、卡时数或预计算成本等定量能耗信息。

### 5. 实验数量与充分性

- **实验组数量**：主实验以两个表格（共18组配置）展示：prefill与decode各9种模型-张量并行组合（如A1、A2、H1、H2、H4）。覆盖了3个模型、2个硬件、5种TP以及128~2048并发和多取值的KV cache压力。
- **充分性评估**：
  - *优点*：多组合横向对比、覆盖模型尺度（8B/14B/32B）、硬件代际（Ampere升级至Hopper）与并行度（TP=1/2/4）、以高百分位（p99）误差而非仅看R²，提高结论可信度。
  - *不足*：主要聚焦于延迟预测精度对比，未见消融（如分别去掉每项特征的贡献）或与ML模型（如Random Forest）的端到端比较；未在真实生产环境远端大批次在线调度下进行部署验证，只测试了vLLM内置profiler的离线路程分析。

### 6. 论文的主要结论与发现

- **prefill阶段**：LLMVisor在全部配置下R²接近1.0（多为1.000），平均p90相对误差0.02、p99相对误差达0.09，相比VTC的平均p90=0.05及p99=0.30，相对误差减少约至1/3。
- **decode阶段**：decode比prefill预测难度更大——VTC的R²甚至低至0.778（Llama3.1-8B+H2），而LLMVisor在所有条件下R²均大于0.97，多数超过0.995；平均p90误差从0.21降至0.06，p99误差从0.44降至0.10（约3.5×和4.4×提升）。
- **效率与可解释性**：模型保持分段线性的简单结构，可直观反映LLM执行的物理性质（如二次注意力与KV cache流量），且运行时开销在微秒级，能满足调度环内部署要求。
- **通用性**：在H100/A100、不同大小模型、多TP配置、混合工作负载下均稳定取得高精确度，支持在多样的生产环境中使用。

### 7. 优点

- **方法可解释性与可归因性**：与纯黑盒ML方法不同，模型系数带有明确计算语义（compute/memory-bound），天然支持线性分解为逐请求cost share，并能支撑计费与公平调度。
- **高效的调度集成能力**：通过在调度循环内以微秒级计算“what-if”预测，为SLO感知准入控制、多租户预留配额等操作提供了实时决策条件。
- **实验设计广泛**：覆盖两代GPU、多个模型大小、4档张量并行度、高负载KV cache压力，性能指标同时包含R²和高百分位误差，体现非平均情况下的稳定性。
- **代码/基准可直接复用**：基于vLLM实现，与主流推理引擎高度契合，便于复制与延展验证。

### 8. 不足与局限

- **缺乏模型/特征消融实验**：未通过“leave-one-feature-out”验证各特征的独立性贡献，无法完全排除特征交叉作用与冗余。
- **基线比较有限**：仅用VTC（token计数）做基线未与Roofline分析ML预测器等先进方案进行基准对比，限制了与现有最优实践的公平横向比较。
- **工作负载为生成/合成数据**：虽然模拟了高度异质序列的KV cache高负载，但未使用公开真实生产trace，生产环境动态性（如抢占、超时、优先级队列）可能降低模型对极端场景的适配能力。
- **部署与评估上未呈现**：论文声明架构可行性，但没有进一步闸述LLMVisor如何在现实中调度实现（如如何在SLO调度器中利用预留配额），也缺少对长时间运行中的在线系数漂移与再校准的讨论。
- **可解释性受限于双线性假设**：本质仍为线性拟合，在非线性严重的共享资源竞争（例如同批不同张量并行规模下的交织干扰）下表现可能退化。
- **算力使用透明度**：虽列出硬件种类与规模，但未报告训练/推理阶段的具体GPU小时数，削弱了全面可复现验证的信息完整度。

### （完）
