---
title: "PATTON: Enabling Commodity PIM for Production LLM Serving"
title_zh: PATTON：为生产级 LLM 服务启用商用 PIM
authors: "Hangyeol Kim, Sanghyun Lee, Teokkyu Suh, Joo-Young Kim"
date: 2026-09-10
pdf: "https://arxiv.org/pdf/2609.11392v1"
tags: ["query:li"]
score: 9.0
evidence: 面向生产级LLM服务管理KV缓存块生命周期的PIM运行时
tldr: 生产级 LLM 服务需在 PIM 上动态管理 KV cache 生命周期，但仅加速注意力不足，Value cache 面临 GEMV 效率、单 token 写入与容量冲突。PATTON 面向商用 PIM：分层粒度分配块级 Key/Value 映射到逻辑 token 块，Commit Zone 暂存 Value 块后提交到 GEMV 优化位置，生成 KV 写入与 QK-transpose/SV 命令。测试平均加速 1.95 倍、能效提升 4.83 倍，KV cache 命中率接近 vLLM GPU KV cache。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.11392v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 884, \"height\": 608, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.11392v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 883, \"height\": 343, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.11392v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1840, \"height\": 381, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.11392v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1843, \"height\": 595, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.11392v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1850, \"height\": 594, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.11392v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1849, \"height\": 636, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.11392v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 888, \"height\": 359, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.11392v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 888, \"height\": 290, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.11392v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1846, \"height\": 437, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.11392v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1845, \"height\": 449, \"label\": \"Table\"}]"
motivation: 生产级 LLM 服务需动态分配、共享、缓存和回收逻辑 KV cache 块，仅加速 decode attention 不足以支撑商用 PIM 部署。
method: PATTON 用分层粒度分配：块级 Key/Value 映射逻辑 token 块，粗粒度分组优化 GEMV，Commit Zone 暂存 Value 块后提交。
result: 评估显示 PATTON 平均加速 1.95 倍、能效提升 4.83 倍，无需修改 PIM 处理单元，KV cache 命中率与 vLLM GPU KV cache 相当。
conclusion: PATTON 将生产 LLM 服务引擎与商用 PIM 集成，解决 Value cache 的 GEMV、写入与容量冲突，为 PIM 上 KV cache 管理提供方案。
---

## 摘要
存内计算（PIM）在加速内存受限的解码注意力方面很有前景，但仅加速注意力不足以支撑生产级 LLM 服务；在生产级 LLM 服务中，引擎会动态分配、填充、共享、缓存和回收逻辑 KV 缓存块。要在商用 PIM 上支持这一生命周期，需要高效的物理内存分配、块到地址映射以及命令生成。对于 Value 缓存，这些需求在 GEMV 效率、单 token 写入效率和内存容量之间造成根本性冲突：面向 GEMV 优化的布局会将新生成的 Value 向量分散到不同行，导致写入开销高昂；而更细粒度的内存共享虽能提升容量利用率，却会碎片化 GEMV 归约。我们提出 PATTON，一种将生产级 LLM 服务引擎与商用 PIM 集成的 PIM 运行时。PATTON 引入分层粒度分配：块大小的 Key 和 Value 粒度与逻辑 token 块一一映射，固定其物理位置和命令；而更粗粒度的粒度将多个块分组，以实现高效的 GEMV 执行和内存利用。Commit Zone 暂存部分 Value 块，以便在将其提交到 GEMV 优化位置之前实现高效的单 token 写入。PATTON 跟踪这些放置位置，以生成 KV 缓存写入以及 QK-transpose/SV 命令。在注意力执行和运行时引发的 prefill 重计算中，PATTON 相比所评估的基线平均实现 1.95 倍加速和 4.83 倍能效提升，无需修改 PIM 处理单元，并保持与 vLLM 中原生 GPU KV 缓存相当的 KV 缓存命中率。

## Abstract
Processing-in-Memory (PIM) is promising for accelerating memory-bound decode attention, but attention acceleration alone is insufficient for production LLM serving, where engines dynamically allocate, populate, share, cache, and reclaim logical KV cache blocks. Supporting this lifecycle on commodity PIM requires efficient physical memory allocation, block-to-address mapping, and command generation. For the Value cache, these requirements create a fundamental conflict among GEMV efficiency, single-token write efficiency, and memory capacity: GEMV-optimized layouts scatter newly generated Value vectors across rows, making writes costly, while finer-grained memory sharing improves capacity utilization but fragments GEMV reductions. We present PATTON, a PIM runtime that integrates production LLM serving engines with commodity PIM. PATTON introduces hierarchical granule allocation: block-sized Key and Value granules map one-to-one to logical token blocks, fixing their physical placements and commands, while coarser granules group blocks for efficient GEMV execution and memory utilization. A Commit Zone stages partial Value blocks for efficient single-token writes before committing them to GEMV-optimized locations. PATTON tracks these placements to generate KV cache writes and QK-transpose/SV commands. Across attention execution and runtime-induced prefill recomputation, PATTON achieves an average 1.95x speedup and 4.83x higher energy efficiency over evaluated baselines, requires no PIM processing-unit modifications, and maintains a KV cache hit rate comparable to the native GPU KV cache in vLLM.

---

## 论文详细总结（自动生成）

# PATTON 论文中文总结

> 说明：以下总结基于给定论文的元数据、摘要与 Abstract。由于未提供正文、实验章节和附录，涉及数据集、基线名称、硬件配置、实验组数等细节只能标注为“未明确”。

## 1. 核心问题与整体含义

- **研究背景**：存内计算（PIM）适合加速内存受限的解码注意力（decode attention），但生产级 LLM 服务不仅需要加速注意力计算。
- **核心问题**：生产级 LLM 服务引擎会动态执行 KV cache 块的生命周期操作，包括分配、填充、共享、缓存和回收。要在商用 PIM 上支持这些操作，需要高效的物理内存分配、块到地址映射以及命令生成。
- **关键矛盾**：对于 Value cache，存在三重冲突：
  - **GEMV 效率**：GEMV 优化布局有利于矩阵向量归约。
  - **单 token 写入效率**：新生成的 Value 向量若按 GEMV 优化布局存放，会被分散到不同行，导致写入开销高。
  - **内存容量**：更细粒度的内存共享可提升容量利用率，但会碎片化 GEMV 归约。
- **整体含义**：仅加速 attention 不足以支撑 PIM 在真实 LLM 服务中的部署。PATTON 试图将生产级 LLM 服务引擎与商用 PIM 集成，为 PIM 上的 KV cache 管理提供完整运行时方案。

## 2. 方法论：核心思想与关键技术

- **核心思想**：提出 PATTON，一种面向商用 PIM 的 PIM 运行时，将生产级 LLM 服务引擎与 PIM 结合，管理 KV cache 的放置、写入和注意力命令生成。
- **分层粒度分配（Hierarchical Granule Allocation）**：
  - **块级粒度**：Key 和 Value 粒度与逻辑 token 块一一映射，固定其物理位置和命令。
  - **粗粒度分组**：将多个块分组，以实现高效的 GEMV 执行和内存利用。
- **Commit Zone 机制**：
  - 用于暂存部分 Value 块。
  - 先支持高效的单 token 写入，再将 Value 块提交到 GEMV 优化位置。
  - 缓解单 token 写入效率与 GEMV 优化布局之间的冲突。
- **命令生成与位置跟踪**：
  - PATTON 跟踪 Key/Value 块的放置位置。
  - 基于这些位置生成 KV cache 写入命令，以及 QK-transpose / SV 命令。
- **兼容性设计**：
  - 不需要修改 PIM 处理单元。
  - 目标是在商用 PIM 上运行，而非依赖定制硬件改动。
- **流程概述（文字版）**：
  - 逻辑 KV cache 块产生后，按块级粒度映射到固定物理位置。
  - 多个块被粗粒度分组，以支持高效 GEMV。
  - 新 Value 先写入 Commit Zone，再提交到 GEMV 优化位置。
  - 运行时根据跟踪的放置信息生成 KV 写入与注意力相关命令。
  - 在注意力执行和 prefill 重计算中复用该管理机制。

## 3. 实验设计

- **评估场景**：
  - 注意力执行（attention execution）。
  - 运行时引发的 prefill 重计算（runtime-induced prefill recomputation）。
- **Benchmark**：
  - 给定文本未明确说明具体数据集、模型、序列长度、并发负载或硬件平台。
  - 仅提到与“所评估的基线”比较，并保持与 vLLM 中原生 GPU KV cache 相当的 KV cache 命中率。
- **对比方法**：
  - 摘要称对比“所评估的基线”，但未列出基线名称。
  - 与 vLLM 原生 GPU KV cache 的 KV cache 命中率进行比较。
- **评价指标**：
  - 加速比。
  - 能效提升。
  - KV cache 命中率。
  - 是否需要修改 PIM 处理单元。

## 4. 资源与算力

- 给定文本未提及以下内容：
  - GPU 型号、数量。
  - 训练时长。
  - PIM 硬件型号、容量、带宽或仿真平台。
  - 服务器配置、功耗测量方式。
- 该工作属于 LLM 推理/服务运行时系统，通常不涉及模型训练算力，但评估所用推理硬件与 PIM 平台同样未明确说明。
- 因此，无法从给定内容判断其算力规模和实验平台真实度。

## 5. 实验数量与充分性

- 给定文本未提供：
  - 实验组数。
  - 数据集数量。
  - 模型规模覆盖。
  - 消融实验。
  - 敏感性分析。
  - 不同序列长度、batch size、并发度下的结果。
- 摘要仅给出平均结果：
  - 平均加速 1.95 倍。
  - 能效提升 4.83 倍。
  - KV cache 命中率与 vLLM 原生 GPU KV cache 相当。
- **充分性判断**：
  - 从摘要层面看，实验覆盖了注意力执行和 prefill 重计算两类场景，并报告了性能、能效和命中率。
  - 但缺少实验细节，无法判断是否充分。
- **客观与公平性判断**：
  - 与基线比较、与 vLLM KV cache 命中率比较，方向合理。
  - 但基线名称、硬件配置、工作负载和测量方法未明，公平性无法充分评估。

## 6. 主要结论与发现

- PATTON 在注意力执行和运行时引发的 prefill 重计算中，相比评估基线平均实现 **1.95 倍加速**。
- 能效平均提升 **4.83 倍**。
- **无需修改 PIM 处理单元**，具备商用 PIM 部署可行性。
- KV cache 命中率与 **vLLM 中原生 GPU KV cache 相当**，说明 PIM 侧 KV cache 管理未显著牺牲缓存效率。
- PATTON 通过分层粒度分配和 Commit Zone，缓解了 Value cache 在 GEMV 效率、单 token 写入效率和内存容量之间的冲突。
- 论文主张：将生产 LLM 服务引擎与商用 PIM 集成，是支持 PIM 上完整 KV cache 生命周期的可行路径。

## 7. 优点

- **面向真实生产服务生命周期**：不只加速 attention，而是覆盖 KV cache 分配、填充、共享、缓存和回收。
- **问题定位清晰**：明确指出 Value cache 在 GEMV 效率、单 token 写入和容量利用之间的根本冲突。
- **方法设计有针对性**：
  - 分层粒度分配兼顾逻辑块映射与 GEMV 效率。
  - Commit Zone 有效分离“高效写入”和“高效计算”两种布局需求。
- **商用兼容性强**：无需修改 PIM 处理单元，降低硬件定制依赖。
- **与生产引擎对齐**：保持与 vLLM 原生 GPU KV cache 相当的命中率，有利于集成到现有服务栈。
- **同时报告性能与能效**：对 PIM 类工作而言，能效提升是关键指标。

## 8. 不足与局限

- **实验细节缺失**：给定文本未列出数据集、模型、序列长度、并发场景、基线名称和硬件配置。
- **实验充分性无法判断**：没有消融实验、敏感性分析、不同模型规模或不同负载下的结果。
- **指标较粗**：仅给出平均加速和平均能效，缺少尾延迟、吞吐、P99 延迟、容量开销等生产级指标。
- **Commit Zone 开销未说明**：暂存和提交机制可能带来额外内存、带宽或管理开销，摘要未量化。
- **命中率比较条件未明**：与 vLLM GPU KV cache 命中率相当，但未说明是否在同等容量、替换策略和请求分布下比较。
- **商用 PIM 限制未展开**：不同 PIM 架构、命令集、内存控制器差异可能影响可移植性。
- **部署生态风险**：与生产 LLM 服务引擎集成时，调度、批处理、抢占、重计算等复杂行为可能带来额外挑战。
- **资源与算力未报告**：
