---
title: Syndrome Decoding for Silent Data Corruption in Quantized Integer GPU Arithmetic
title_zh: 量化整数 GPU 算术中静默数据损坏的伴随式译码
authors: "Pranav Napolean, Vikas Srivastava, Napolean Periathambi"
date: 2026-09-17
pdf: "https://arxiv.org/pdf/2609.19743v1"
tags: ["query:li"]
score: 6.0
evidence: 面向GPU张量核量化整数GEMM的验证内核
tldr: "量化神经网络在 GPU 张量核上执行整数矩阵乘法推理，其 INT32 累加器没有校验，瞬时故障会返回看似有效却错误的整数，造成不报错的静默数据损坏。现有校验和 ABFT 只能给出二元判决，无法定位出错元素与幅值，且对行列上相互抵消的错误天然失明。SProbe 作为尾随验证核读取未修改厂商 GEMM 的输出，先用 61 位素数域上的随机 Freivalds 门以至多 2^-141 的漏检率筛查，再用三素数幂和伴随式及 Reed-Solomon 链解码，定位并修复每行最多四个碰撞错误。在 H100 上，它覆盖七类故障和四种矩阵规模并全部检出，在 INT8 医疗 LLM 中消除全部观测静默损坏，吞吐代价约 30%。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.19743v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 852, \"height\": 549, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.19743v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 878, \"height\": 543, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.19743v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 866, \"height\": 268, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.19743v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 792, \"height\": 192, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.19743v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 778, \"height\": 229, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.19743v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 832, \"height\": 359, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.19743v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 713, \"height\": 285, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.19743v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 792, \"height\": 408, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.19743v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 911, \"height\": 260, \"label\": \"Table\"}]"
motivation: 量化推理依赖无 ECC 的 INT32 累加器，瞬时故障可产生不报错的静默数据损坏，而现有 ABFT 只能检测、不能定位与纠错。
method: SProbe 读取未修改厂商 GEMM 的输出，先用三评估点 61 位素数域 Freivalds 门筛查，再以三素数幂和伴随式及 Reed-Solomon 解码定位并修复每行最多四个碰撞错误。
result: "在 H100 上七类故障、四种矩阵规模全部检出；N=16384 时门开销为 cuBLASLt GEMM 的 49%，N=65536 时 11%；INT8 医疗 LLM 消除全部观测静默损坏，吞吐代价 30%。"
conclusion: SProbe 可诊断并修复量化 GEMM 静默错误，实验还表明重算快于原地恢复，诊断而非修复主导恢复成本，并暴露验证器自身缺陷。
---

## 摘要
量化神经网络推理在 GPU Tensor Core 上执行整数矩阵乘法，而这些核心内部的 INT32 累加器既无奇偶校验也无 ECC。该数据通路中的瞬态故障会返回一个合法但错误的整数，且不触发中断。基于校验和的算法级容错（ABFT）可以检测此类静默数据损坏（SDC），但其判定结果是二元的。它无法识别出错元素或其幅度，并且无权重的行、列校验和从构造上就对在两个轴上相互抵消的错误不敏感。我们提出 SProbe，一种后置验证内核，读取未经修改的厂商 GEMM 的输出。一个随机化 Freivalds 门控在 61 位素数域中使用三个独立求值点，漏检非零错误的概率至多为 $2^{-141}$。当该门控触发时，对三个素数上的逐行幂和伴随式，使用由 Berlekamp-Massey、Chien 搜索和 Forney 组成的 Reed-Solomon 链进行译码，可恢复每行至多四个碰撞错误的列位置和精确幅度。随后 SProbe 原地修复累加器，或重新计算 GEMM。在 NVIDIA H100 上，SProbe 在七类故障和四种矩阵尺寸下检测出所有注入故障，包括 TR-ABFT 从未检测到的构造模式，以及加权网格码能检测但无法纠正的模式。该门控在 N=16384 时带来 cuBLASLt GEMM 时间 49% 的开销，在 N=65536 时为 11%。在一个 INT8 医学 LLM 中，保护机制以 30% 的吞吐量开销消除了所有观察到的静默损坏。我们的测量还表明，在我们测试的每种配置中，重新计算都比原地恢复更快；诊断而非修复主导了恢复开销；并且我们报告了在验证验证器本身时发现的缺陷。

## Abstract
Quantized neural network inference runs integer matrix multiplications on GPU tensor cores, and the INT32 accumulators inside those cores have neither parity nor ECC. A transient fault in this datapath returns a valid but wrong integer and raises no interrupt. Checksum based Algorithm Based Fault Tolerance (ABFT) can detect such silent data corruptions (SDCs), but its verdict is binary. It cannot identify the corrupted element or its magnitude, and unweighted row and column checksums are blind by construction to errors that cancel on both axes. We present SProbe, a trailing verification kernel that reads the output of an unmodified vendor GEMM. A randomized Freivalds gate with three independent evaluation points in a 61 bit prime field misses a nonzero error with probability at most $2^{-141}$. When the gate fires, per row power sum syndromes over three primes are decoded with the Reed Solomon chain of Berlekamp Massey, Chien search, and Forney, recovering the column and exact magnitude of up to four colliding errors per row. SProbe then repairs the accumulator in place or recomputes the GEMM. On an NVIDIA H100, SProbe detects every injected fault across seven fault classes and four matrix sizes, including constructed patterns that TR-ABFT never detects and patterns that a weighted grid code detects but cannot correct. The gate costs 49% of the cuBLASLt GEMM time at N=16384 and 11% at N=65536. In an INT8 medical LLM, protection eliminates all observed silent corruptions at a 30% throughput cost. Our measurements also show that recomputation is faster than in place recovery in every configuration we tested, that diagnosis rather than repair dominates recovery cost, and we report the defects we found while validating the verifier itself.