---
title: "The Art of Closed-Formula Defaults: Search-Free Code Generation for Tensor Operators"
title_zh: 闭式默认值的艺术：面向张量算子的免搜索代码生成
authors: "Paolo D'Alberto, Ashish Sirasao"
date: 2026-09-10
pdf: "https://arxiv.org/pdf/2609.10937v1"
tags: ["query:li"]
score: 8.0
evidence: 面向张量算子的免搜索GPU算子代码生成
tldr: 针对LLM推理中的GPU张量算子，现有智能体搜索与自动优化虽强，但其效果更取决于优化问题定义是否清晰，而非搜索本身复杂精妙。本文提出以应用为先的无搜索代码生成方法，借助闭式默认值驱动分层代码生成工具，从算子规格逐层下降到GPU指令。研究表明，清晰的计算模型能让优化问题变得可解，为张量算子生成提供更直接高效的路径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.10937v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1727, \"height\": 873, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.10937v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 754, \"height\": 482, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.10937v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 911, \"height\": 192, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.10937v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 702, \"height\": 305, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.10937v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 775, \"height\": 280, \"label\": \"Table\"}]"
motivation: LLM推理依赖GPU内核，但智能体搜索与自动优化成本高，且效果受限于优化问题定义是否清晰。
method: 提出应用优先方法，用闭式默认值实现无搜索代码生成，将算子规格经分层工具逐层降低至GPU指令。
result: 表明清晰计算模型可让优化问题可解，无需复杂搜索即可生成张量算子代码。
conclusion: 强调问题定义优先于搜索技巧，为张量算子代码生成提供免搜索、可解释的工程路径。
---

## 摘要
智能体搜索与 GPU 内核自动优化是大型语言模型推理的强大工具。然而，它们的有效性并不取决于搜索本身的复杂程度，而取决于所求解优化问题的清晰程度。我们提出一种应用优先的方法，驱动分层代码生成工具从算子规范一直向下到 GPU 指令，并表明明确定义的计算模型使优化问题变得可处理。

## Abstract
Agentic search and automated optimization of GPU kernels are powerful tools for large language model inference. Their effectiveness, however, depends not on the sophistication of the search itself, but on the clarity of the optimization problem being solved. We provide an application-first approach that drives a hierarchical code generation tool from operator specifi cation down to GPU instructions, and show that a clearly defined computational model makes the optimization problem tractable.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义

- **研究动机**：LLM 推理依赖高性能 GPU 张量算子。现有 cuDNN、hipBLAS、Triton、FlashAttention 等主要通过 autotuning / agentic search 搜索 tile 大小、寄存器分块和内存布局，但搜索成本高，且效果高度依赖优化问题是否被清晰定义。
- **核心问题**：能否不依赖搜索，而是从算子规格和硬件描述中解析地推导出正确、可部署的 tile 配置？
- **整体含义**：论文主张“问题定义优先于搜索技巧”。若把张量算子描述为分层计算树，并用精简硬件描述符刻画 LDS、VGPR、CU 数、MFMA 指令形状等关键资源，则 tile 大小可由闭式公式确定。得到的默认实现“构造即正确”，可作为后续自动搜索的 warm start。

## 2. 方法论：核心思想与关键技术

### 2.1 核心思想

- **应用优先、CU 中心、免搜索**：以 compute unit 为锚点，因为 CU 拥有两个绑定资源：local data share 和 vector register。
- **分层算法树 + visitor**：算子被描述为树，例如 device → CU/block → wavefront/warp → MFMA atom。`ComputeVisitor` 用于验证，`EmitVisitor` 用于生成 GPU kernel；二者共享同一棵树，保证“验证的即编译的”。
- **tile policy 与 boundary policy 分离**：tile 大小由解析推导决定；边界用 min/max 裁剪，GEMM 零 padding 对累加无影响。

### 2.2 硬件抽象

- `HardwareDescriptor` 编码：LDS 字节数、每线程 VGPR、wavefront 大小、CU 数、HBM 带宽、峰值算力、可用 MFMA atom。
- `MFMAAtom` 编码：指令形状 `(m,n,k)`、输入/累加 dtype，以及实际 fragment 布局。
- 换硬件只需换 descriptor，tile 推导自动重跑。

### 2.3 闭式 tile 推导流程

按固定顺序施加三类约束：

1. **LDS 约束 ⇒ CU tile**  
   对 GEMM，CU 同时载入 A、B tile，需满足  
   `(tM + tN) × tK × elem ≤ LDS`。  
   取 `tM = tN = floor(sqrt(LDS/(2·elem)))`，再求 `tK`。  
   在 64 KB LDS、4 字节元素下得到 `tM=tN=128, tK=32`。

2. **VGPR 约束 ⇒ warp tile**  
   每个线程保存 `(twm×twn)/wavefront_size` 个累加器浮点数。  
   对 MFMA `(m,n,c)`，用 `V(u)=u·twn/n·c + Voverhead ≤ Vbudget` 求最大 warp tile，并对齐 atom 形状
