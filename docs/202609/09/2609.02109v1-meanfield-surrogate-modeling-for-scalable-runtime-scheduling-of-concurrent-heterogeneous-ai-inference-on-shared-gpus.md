---
title: MeanField Surrogate Modeling for Scalable Runtime Scheduling of Concurrent Heterogeneous AI Inference on Shared GPUs
title_zh: 平均场代理建模用于共享GPU上并发异构AI推理的可扩展运行时调度
authors: "Youssef Ennouri, Soonhoi Ha"
date: 2026-09-02
pdf: "https://arxiv.org/pdf/2609.02109v1"
tags: ["query:li"]
score: 9.0
evidence: 用平均场代理模型支持共享GPU上并发异构AI推理的可扩展运行时调度
tldr: "共享GPU上并发执行异构AI推理时，资源竞争严重制约运行时调度性能；现有代理模型需组合式配置采样，成本随模型数爆炸。本文提出平均场代理模型，从聚合GPU状态与局部配置预测单个模型性能，采样复杂度由组合型降为近线性。集成遗传算法调度器后，在N=5的78,732种可行配置下预测R²≈0.96，最优解相对穷举搜索误差低于0.1%，动态场景下SLA违规为零，在线决策中位数仅26毫秒，比穷举快约5倍。这表明平均场建模为共享GPU场景中的可扩展运行时调度提供了高效实用方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.02109v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 899, \"height\": 371, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.02109v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 959, \"height\": 700, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.02109v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 822, \"height\": 603, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.02109v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 562, \"height\": 261, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.02109v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 889, \"height\": 323, \"label\": \"Table\"}]"
motivation: 共享GPU上并发异构模型调度因资源竞争复杂，现有替代模型配置成本组合爆炸，缺乏可扩展性。
method: 提出平均场代理模型，从聚合GPU状态与局部配置预测每个模型性能，样本预算随模型数近似线性，并集成遗传算法调度器。
result: "实验覆盖N=2至6，R²约0.96，样本预算近线性；N=5的78,732配置下，性能与穷举搜索相差不足0.1%，八个场景零SLA违规，GA中位决策26毫秒，提速近5倍。"
conclusion: 平均场代理建模以线性增长成本实现高精度并发调度，为共享GPU异构AI推理提供可扩展且实用的运行时方案。
---

## 摘要
在共享GPU上并发部署异构AI模型会引入资源争用，从而使运行时调度复杂化。尽管代理模型可以避免昂贵的在线基准测试，但它们的性能剖析开销通常随并发运行模型数量呈组合式增长，这限制了可扩展性。我们提出一种平均场代理模型，它根据局部配置和GPU聚合状态预测每个模型的性能，而不是显式地对所有联合交互进行建模。针对N∈{2,3,4,5,6}的并发LLM与视觉工作负载进行的实验表明，所提方法预测精度高（R²≈0.96），经验样本预算随N近似线性增长，与完整联合剖析的组合开销形成对比。将该代理模型集成到遗传算法调度器后，能够扩展到具有78,732个可行联合配置的N=5问题，与穷举搜索的差距保持在0.10%以内，并在八个动态工作负载场景中零SLA违规；完整的在线遗传算法决策中位时间仅为26毫秒，比穷举代理搜索快约5倍。

## Abstract
Deploying heterogeneous AI models concurrently on a shared GPU introduces resource contention that complicates runtime scheduling. While surrogate models avoid costly online benchmarking, their profiling requirements typically grow combinatorially with the number of co-running models, limiting scalability. We propose a MeanField surrogate that predicts per-model performance from local configuration and aggregate GPU state rather than explicitly modeling all joint interactions. Experiments on concurrent LLM and vision workloads across $N \in \{2,3,4,5,6\}$ show high predictive accuracy ($R^2 \approx 0.96$) with an empirical sample budget that grows approximately linearly in $N$, in contrast to the combinatorial cost of fully joint profiling. Integrated into a genetic algorithm scheduler, the surrogate scales to an $N=5$ problem with 78,732 feasible joint configurations, remaining within 0.10% of the exhaustive search with zero SLA violations across eight dynamic workload scenarios, while complete online GA decisions take 26 ms median, about $5\times$ faster than exhaustive surrogate search.

---

## 论文详细总结（自动生成）

以下是基于论文内容生成的结构化中文总结：

## 1. 论文的核心问题与整体含义

### 研究背景
- 现代推理部署越来越多地在共享 GPU 上共置异构 AI 工作负载（如 LLM、视觉模型），以提升资源利用率。
- 并发执行会产生严重的资源竞争：SM 占用、视频内存容量、内存带宽等。
- 干扰程度取决于模型变体、运行时参数及工作负载强度等因素，呈现非线性特性，使分析性建模不切实际。
- 既有系统如 Nexus、Clipper、Clockwork、Orion 等或限于同构/隔离工作负载，或受扩展性瓶颈制约。

### 核心问题
- 现有代理模型预测并发 N 模型联合配置，性能剖析预算随 N 呈组合爆炸式增长，即 O(k^N) 级别。
- 在真实异构 GPU 推断场景中，全联合配置空间导致在线调度在计算上不可行。

### 整体含义
- 论文提出平均场代理模型，将交互干扰问题分解为“本地配置 + GPU 聚合状态”，回避逐模型联合建模，把采样复杂度从组合级降至近似线性，为共享 GPU 异构推断提供可扩展的在线调度方案。

## 2. 论文提出的方法论

### 核心思想：平均场近似（MeanField）启发
- 类比统计物理中的平均场近似：取代逐一建模多体交互，改用单个聚合“场”表示综合压力。
- 此处，聚合 GPU 状态 (mt, ut, ρt) 充当各并行模型综合资源压力的替身，建模的动机是源于方差分析揭示干扰主要由总体资源压力而非特定模型配对驱动。

### 系统架构与调度流程
- 运行时观测状态 st = (ρt, mt, ut)，包括工作负载水平、VRAM 用量和 GPU 整体利用率。
- 调度器在每个步长选出最优联合配置 λ* = argmax J(λ, st)，由代理模型 + GA 搜索完成。

### 代理模型公式
- 每个模型配一个独立轻量 predictor：
  fi^ = gi(λi, mt, ut, ρt)
- 特征：本地配置 λi（如 LLM 变体和并发度、YOLO 变体和 skip rate、输入大小）+ 聚合 GPU 状态。
- 输出：模型级吞吐预测；按变体做 min-max 归一化。

### 代理模型架构
- 每模型一个小型 64-32 MLP：用分类特征作序数编码，采用 BatchNorm、ReLU、Dropout(0.1) 和 Sigmoid 输出层。
- 该结构经 N=3 且 n=80 的消融实验选取，兼顾精度与推理延迟（亚毫秒级本地推断）。
- 单次预测对本地配置集合的中位耗时 8.5 ms。

### 调度目标与约束
- 效用函数： J = Σi ωifiQi − α Σ ηi cref_i − β Σ max(0, τi + δ − fi)²
- 第一项：吞吐加权质量分数
- 第二项：重配置成本；α=1/60 作为 60 秒摊销
- 第三项：SLA 惩罚，用 δ=0.05 安全余量
- 硬约束：VRAM 之和 ≤ 总可用容量 R，fi ≥ τi（吞吐下限）
- 违反约束的候选得到 J = −∞，不可行变异个体用随机可行个体修复。

### 离线剖析
- N=2 基线系统枚举全部 162 个联合模型配置并在 4 种工作负载（低、中、高、突发）下执行，共 648 个剖析测量点。
- 每单元重复测量 10 次（丢弃 2 次预热），固定 GPU 时钟、温度稳定后记录。
- 对 N>2 采用分层采样（依模型族、变体和负载水平），而非全笛卡尔枚举。
- 干扰因子：Δi = f_isolated_i − f_concurrent_i。

### 可扩展性
- 联合代理需要 O(k^N) 的成本层，输入维度随 N 增长。
- 平均场方法仅需 O(N·k) 的分布配额，在图 3 中经验为 n* ≈ 20N 个样本。

### 遗传算法调度器
- 100 个候选、交叉概率 0.7、突变概率 0.1、二元锦标赛 K=3、精英选择、30 代。
- VRAM 不可行配置在构建 Λ 时即被滤除。
- 在 N=5 时，GA 搜索在预测后占用 17 ms，中位全链路决策延迟为 26 ms。

## 3. 实验设计

### 实验平台与 workload
- 硬件：单个 NVIDIA RTX 3090，24 GB GDDR6X；CUDA 12.6；Ubuntu 20.04；LLM 经 vLLM 服务，视觉模型使用 Ultralytics 引擎。
- LLM：Qwen2.5 系列，变体为 FP16 与 AWQ，每模型并发数 {1, 2, 4}，共 6 种本地配置。
- 视觉：YOLO11 系列，变体为 n/s/m，skip rate ∈ {1, 2, 4}、imgsz ∈ {320, 480, 640}，共 27 种本地配置。
- 工作负载情形：低、中、高、突发，共 4 种状态。

### 对比方法
- N=2 基线：Joint MLP 全联合代理（参照系）
- 穷举搜索
- StaticFull：完整配置空间内的固定配置
- StaticBudget(5000) / StaticBudget(500)：使用 K=5000 或 500 条离线样本选择一个固定配置
- Random 随机调度

### 实验内容
- N ∈ {2, 3, 4, 5, 6} 的样本复杂度扩展性：绘制学习曲线，求收敛样本数 n*
- 模型族：LLM 和视觉模型混合：N=5 时为 3 个 LLM + 2 个 YOLO 模型，跨 3 个 LLM 可变本地配置。
- 调度验证：8 个动态工作负载场景 × 3 个随机种子，SLA 基准设为各模型归一化性能的 P25。
- 评估指标：效用 J、平均差距、最差差距、SLA 违规率。

### 对比优势说明
- n* ≈ 20N 个训练样本即可在 R² ≥ 0.95 时收敛——远低于全联合所需的 O(kN)。

## 4. 资源与算力

- GPU：单个 NVIDIA RTX 3090（24 GB GDDR6X）
- 软件：CUDA 12.6、Ubuntu 20.04、vLLM、Ultralytics、PyTorch
- 测量条件：固定 GPU 时钟、热温度低于 70°C、屏障同步启动
- 论文给出了经验样本预算和每单元 10 次重复，但未报告总剖析墙钟时间/GPU 时长，也未列出 GA 响应时间，但报告为 26 ms 中位决策延迟
- 未说明完整训练所用分钟/小时数，或精确剖析成本（工作日）
- 提到 N=7B 的 LLM 只能使用 AWQ 变体，因为 24 GB 显存不足，限制更大模型的测试。

## 5. 实验数量与充分性

### 实验数量概况
- 样本复杂度：N ∈ {2,3,4,5,6} 多组学习曲线评估，每组 5 折×5 个种子 = 25 次重复
- 剖析：N=2 配置空间 162 种联合 × 4 种工作负载 = 648 个单元，每单元 10 次重复
- 调度验证：8 个动态场景×3 个种子，并对比 5 个基线（穷举、StaticFull、Budget5000、Budget500、Random）
- 预测精度评估：表 I 给出多 N 值 R² ± 标准差，含 25 次重复

### 充分性与公平性
- 评价维度充分：覆盖了 R² 精度、样本收敛性和端到端调度效用差异
- 公平性良好：场景相同、比较一致，穷举以稳定上界为参照
- 注重统计可靠性：多次随机种子和交叉验证

### 局限性
- 动态场景仅 8 个，属于小规模情景
- 没有与最新的干扰感知调度器（如 Orion、ElasticRoom）进行对比，只比较了在代理支持下的穷举/静态/随机
- 两个模型族仅代表一小部分实际异构工作负载
- SLA 实测假设预测准确，因此 0% 违规主要是架构性质的，因穷举层基于同一代理

## 6. 论文的主要结论与发现

- 平均场代理在 N=2 到 6 一直保持高精度：R² ≈ 0.96 - 0.986，且以经验线性样本预算（n* ≈ 20N）运行，而非联合剖析的组合级成本
- 干扰主要由聚合资源压力和负载等级驱动，而非特定模型配对，验证了平均场假设的合理性
- 剖析显示 AWQ 变体平均经受 8.4% 的吞吐损失；视觉模型损失约 17-24% 的 FPS，取决于模型尺寸
- 在 N=5、78,732 个可行联合配置中，GA 保持在穷举的 0.10% 以内竞争范围，并保持零 SLA 违规
- GA 的端到端中位决策时间为 26 ms，比穷举的 131 ms 约快 5 倍
- 静态配置性能差得多（对 5000 和 500 个校准点的平均差距分别为 5.22% 和 10.51%），表明离线配置受预算约束时效能会下降
- 相对静态方案，主要优势是效率可扩展的调度，而非动态适应本身。

## 7. 优点

- **扩展性强的核心方案**：平均场近似的迁移使配置成本由组合级 O(kN) 降为近线性 O(N·k)；方法简洁而优雅
- 实证严谨：在标定时钟与温度下测量并做验证；25 次重复实验增强可靠性
- 高效协同设计：小型专用 MLP（64-32）实现亚毫秒级推理，几乎不含延迟
- 实用调度配方：明确建模重配置成本、变体重载及基于 P25/余量优先的 SLA 硬约束与软惩罚
- 公平性设计：按变体做 min-max 归一化，避免快速变体遮蔽慢速变体
- 清晰的比较解剖层次：针对代理抽样归一化，区分搜索质量与预测质量

## 8. 不足与局限

- **硬件规模有限**：仅在单个 GPU 上进行评估；无多 GPU、无 CPU/GPU 异构集群，无通用性证明
- **覆盖面上仅两类模型**：仅 Qwen2.5 的 LLM 与 YOLO11 视觉模型，未覆盖 transformer 视觉模型、语音、推荐或文本编码等
- **动态场景较少**：8 个场景且均以一次仿真/回放模式展示；无实际集群并发模式
- **SLA 测量间接**：仅覆盖代理预测的效用，未讨论真实的工作流层面
- **未涉及运行时状态变化预测**：代理依赖当前观测，无法预测未来 GPU 状态演化，也未对 KV-cache 动态进行建模
- **未提供训练成本**：未报告训练时长/能耗
- **应用范围窄**：缺乏与最新的干扰感知调度器（Nexus/Orion/DVFS 感知启发式）的对比

（完）
